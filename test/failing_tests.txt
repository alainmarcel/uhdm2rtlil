# Failing tests that are expected to fail
# Format: one test directory name per line
# Comments start with #

# Tests listed here will be marked as expected failures in CI
# The test runner will pass if all failures are listed here

# Yosys upstream tests use `module ref (...)` — `ref` is a reserved
# keyword in IEEE 1800-2017 (function-arg direction).  Surelog
# correctly rejects it; Yosys's verilog frontend doesn't tokenize
# `ref` as a keyword so it accepts the file.  Covered locally by
# test/load_and_derive/ and test/resolve_types/ with `ref` renamed
# to `ref_mod`.
svinterfaces/load_and_derive.sv
svinterfaces/resolve_types.sv

# const_arg_loop's operation2 has a function-local `inp[0] = inp[0] ^ 1`
# bit-select self-assignment followed by `operation2 = num * inp`.
# Yosys's verilog frontend splits the partial write into per-bit-range
# connects (bit 0 = xor result, bits [4:1] = original constant), but
# our UHDM frontend emits the actions as sequential proc actions and
# the multiplication ends up reading the original constant value of
# `inp` — giving x2 = 6 instead of 4.  The other four outputs (x1,
# x1b, x3, x4) pass via the loop-chain init / concat-width fixes.
# Tracked as a deeper sequential-update-in-function-body bug.
const_arg_loop
