read_uhdm slpp_all/surelog.uhdm
hierarchy -check -top genvar_nested_func_loopvar
flatten; proc; opt; memory; async2sync; techmap; opt
rename genvar_nested_func_loopvar gold
design -stash gold
read_slang --ignore-assertions dut.sv --top genvar_nested_func_loopvar
hierarchy -check -top genvar_nested_func_loopvar
flatten; proc; opt; memory; async2sync; techmap; opt
rename genvar_nested_func_loopvar gate
design -stash gate
design -copy-from gold -as gold gold
design -copy-from gate -as gate gate
miter -equiv -flatten -make_assert gold gate miter
hierarchy -top miter
sat -verify -prove-asserts -seq 1 -set-init-zero miter
