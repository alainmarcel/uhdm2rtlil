read_uhdm slpp_all/surelog.uhdm
hierarchy -check -top unpacked_struct_2d
flatten; proc; opt -fast; async2sync; setundef -undriven -zero
rename unpacked_struct_2d gold
design -stash gold
read_slang dut.sv --top unpacked_struct_2d
hierarchy -check -top unpacked_struct_2d
flatten; proc; opt -fast; async2sync; setundef -undriven -zero
rename unpacked_struct_2d gate
design -stash gate
design -copy-from gold -as gold gold
design -copy-from gate -as gate gate
miter -equiv -flatten -make_assert gold gate miter
sat -verify -prove-asserts -seq 8 -set-init-zero -show-inputs -show-outputs miter
