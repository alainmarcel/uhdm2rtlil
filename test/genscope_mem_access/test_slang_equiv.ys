read_uhdm slpp_all/surelog.uhdm
hierarchy -check -top genscope_mem_access
flatten; proc; opt; memory; async2sync; techmap; opt
rename genscope_mem_access gold
design -stash gold
read_slang --ignore-assertions dut.sv --top genscope_mem_access
hierarchy -check -top genscope_mem_access
flatten; proc; opt; memory; async2sync; techmap; opt
rename genscope_mem_access gate
design -stash gate
design -copy-from gold -as gold gold
design -copy-from gate -as gate gate
miter -equiv -flatten -make_assert gold gate miter
hierarchy -top miter
sat -verify -prove-asserts -seq 3 -set-init-zero miter
