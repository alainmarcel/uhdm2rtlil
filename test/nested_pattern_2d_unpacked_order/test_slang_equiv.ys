read_uhdm slpp_all/surelog.uhdm
hierarchy -check -top nested_pattern_2d_unpacked_order
flatten; proc; opt -fast; async2sync; setundef -undriven -zero
rename nested_pattern_2d_unpacked_order gold
design -stash gold
read_slang dut.sv --top nested_pattern_2d_unpacked_order
hierarchy -check -top nested_pattern_2d_unpacked_order
flatten; proc; opt -fast; async2sync; setundef -undriven -zero
rename nested_pattern_2d_unpacked_order gate
design -stash gate
design -copy-from gold -as gold gold
design -copy-from gate -as gate gate
miter -equiv -flatten -make_assert gold gate miter
hierarchy -top miter
sat -verify -prove-asserts -seq 4 -set-init-zero miter
