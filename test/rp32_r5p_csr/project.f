# rp32 r5p_csr — generated by import_design.py from test/rp32.
# Ordered package deps + the module; shared RTL in ../rp32/.
# NOTE: r5p_csr is WIP/INCOMPLETE upstream (jeras/rp32) — it references
# `dec_csr_t`/`dec_priv_t` and CSR-map members (utvec/ucause/uepc) that are
# typedef'd NOWHERE in the upstream project (any branch or submodule).  Upstream
# sim/sources-degu.mk COMMENTS OUT r5p_csr.sv + r5p32_csr_pkg.sv, so it is never
# compiled in any buildable config.  Surelog (read_uhdm) is lenient and imports
# it (missing members -> X); read_slang correctly rejects it, so there is no
# slang miter (classified "upstream RTL incomplete").  Kept as a uhdm-only
# import test only; NOT re-vendorable (nothing correct exists upstream).
# top: r5p_csr
# mode: uhdm-only

../rp32/degu/r5p_pkg.sv
../rp32/riscv/riscv_isa_pkg.sv
../rp32/riscv/riscv_priv_pkg.sv
../rp32/riscv/rv32_csr_pkg.sv
../rp32/degu/r5p32_csr_pkg.sv
../rp32/riscv/rv64_csr_pkg.sv
../rp32/degu/r5p64_csr_pkg.sv
../rp32/riscv/riscv_isa_i_pkg.sv
../rp32/degu/r5p_csr.sv
