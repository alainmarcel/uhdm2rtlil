# Test script to read Verilog file directly in Yosys
read_verilog -sv dut.sv
# Write RTLIL immediately after reading, before hierarchy
write_rtlil carryadd_from_verilog_nohier.il
hierarchy -check -top carryadd
stat
proc
opt
stat
write_rtlil carryadd_from_verilog.il
# Synthesize to gate-level netlist
synth -top carryadd
write_verilog -noexpr carryadd_from_verilog_synth.v
