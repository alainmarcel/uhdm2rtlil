read_uhdm slpp_all/surelog.uhdm
hierarchy -check -top struct_member_dyn_indexed_partsel
flatten; proc; opt -fast; async2sync; setundef -undriven -zero
rename struct_member_dyn_indexed_partsel gold
design -stash gold
read_slang dut.sv --top struct_member_dyn_indexed_partsel
hierarchy -check -top struct_member_dyn_indexed_partsel
flatten; proc; opt -fast; async2sync; setundef -undriven -zero
rename struct_member_dyn_indexed_partsel gate
design -stash gate
design -copy-from gold -as gold gold
design -copy-from gate -as gate gate
miter -equiv -flatten -make_assert gold gate miter
hierarchy -top miter
sat -verify -prove-asserts -seq 4 -set-init-zero -set-def-inputs -show-all miter
