# UHDM-vs-slang miter for typaram_pav_dims.  read_verilog does not support
# `parameter type`, so the golden reference is the built-in read_slang
# frontend — the same reference used for CVA6 module equivalence.
read_uhdm slpp_all/surelog.uhdm
hierarchy -check -top typaram_pav_dims
flatten; proc; opt; techmap; opt
rename typaram_pav_dims gold
design -stash gold

read_slang dut.sv --top typaram_pav_dims
hierarchy -check -top typaram_pav_dims
flatten; proc; opt; techmap; opt
rename typaram_pav_dims gate
design -stash gate

design -copy-from gold -as gold gold
design -copy-from gate -as gate gate
miter -equiv -flatten -make_assert gold gate miter
hierarchy -top miter
sat -verify -prove-asserts -seq 4 -set-init-zero miter
