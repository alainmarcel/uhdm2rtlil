prefix="/usr/local"
exec_prefix="${prefix}"
libdir="/usr/local/lib"
includedir="/usr/local/include"

Name: SURELOG
Description: 
Version: 1.84
# Currently, Surelog and UHDM version numbers go in tandem
Requires: UHDM >= 1.84
Cflags: -I${includedir}
Libs: -L${libdir} -lsurelog -lantlr4-runtime -lz
